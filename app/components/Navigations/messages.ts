/*
 * Navigations Messages
 *
 * This contains all the text for the Navigations component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Navigations';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Navigations component!',
  },
});
