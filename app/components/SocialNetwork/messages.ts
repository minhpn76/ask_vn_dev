/*
 * SocailNetwork Messages
 *
 * This contains all the text for the SocailNetwork component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.SocailNetwork';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SocailNetwork component!',
  },
});
