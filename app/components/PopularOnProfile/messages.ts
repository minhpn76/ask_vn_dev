/*
 * PopularOnProfile Messages
 *
 * This contains all the text for the PopularOnProfile component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.PopularOnProfile';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PopularOnProfile component!',
  },
});
