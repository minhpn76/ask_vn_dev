/*
 * CheckListAccount Messages
 *
 * This contains all the text for the CheckListAccount component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CheckListAccount';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CheckListAccount component!',
  },
});
