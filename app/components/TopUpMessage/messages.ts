/*
 * TopUpMessage Messages
 *
 * This contains all the text for the TopUpMessage component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.TopUpMessage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TopUpMessage component!',
  },
});
