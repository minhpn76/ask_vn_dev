/*
 * MapsCard Messages
 *
 * This contains all the text for the MapsCard component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.MapsCard';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MapsCard component!',
  },
});
