/*
 * LocationAroundMe Messages
 *
 * This contains all the text for the LocationAroundMe component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.LocationAroundMe';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the LocationAroundMe component!',
  },
});
