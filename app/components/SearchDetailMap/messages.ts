/*
 * SearchDetailMap Messages
 *
 * This contains all the text for the SearchDetailMap component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.SearchDetailMap';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SearchDetailMap component!',
  },
});
